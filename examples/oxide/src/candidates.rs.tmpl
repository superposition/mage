// @generated by scripts/evolve.py -- do not edit by hand.
// wrapper template sha256 {{WRAPPER_SHA256}} | body template sha256 {{BODY_SHA256}}
// generation {{GENERATION}}
//
// Roles: `best` is the loop's incumbent configuration, `new` is the proposed
// candidate, both rendered from the same template. Select one with input.json
// "variant": "best" | "new"; a manifest without `variant` runs the committed
// kernels in this file's sibling `kernels` module.
#[cuda_module]
mod candidates {
    use super::*;

    /// (block_m, block_n, k_step) for `matmul_best`.
    pub const MATMUL_TILE_BEST: (u32, u32, u32) = ({{BEST_BM}}, {{BEST_BN}}, {{BEST_BK}});
    /// (threadIdx.x, threadIdx.y) for `matmul_best`.
    pub const MATMUL_BLOCK_BEST: (u32, u32) = ({{BEST_BX}}, {{BEST_BY}});
    /// (rows per block, threads per block) for `layernorm_best`.
    pub const LAYERNORM_CFG_BEST: (u32, u32) = ({{BEST_LN_ROWS}}, {{BEST_THREADS}});
    pub const BEST_MATMUL_PARAMS_JSON: &str = r##"{{BEST_MATMUL_PARAMS_JSON}}"##;
    pub const BEST_LAYERNORM_PARAMS_JSON: &str = r##"{{BEST_LAYERNORM_PARAMS_JSON}}"##;

    /// (block_m, block_n, k_step) for `matmul_new`.
    pub const MATMUL_TILE_NEW: (u32, u32, u32) = ({{NEW_BM}}, {{NEW_BN}}, {{NEW_BK}});
    /// (threadIdx.x, threadIdx.y) for `matmul_new`.
    pub const MATMUL_BLOCK_NEW: (u32, u32) = ({{NEW_BX}}, {{NEW_BY}});
    /// (rows per block, threads per block) for `layernorm_new`.
    pub const LAYERNORM_CFG_NEW: (u32, u32) = ({{NEW_LN_ROWS}}, {{NEW_THREADS}});
    pub const NEW_MATMUL_PARAMS_JSON: &str = r##"{{NEW_MATMUL_PARAMS_JSON}}"##;
    pub const NEW_LAYERNORM_PARAMS_JSON: &str = r##"{{NEW_LAYERNORM_PARAMS_JSON}}"##;

{{BODIES}}
}
